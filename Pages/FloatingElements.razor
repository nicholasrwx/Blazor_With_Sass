@page "/floatingelements"

<PageTitle>Floating Elements</PageTitle>

<h1>Floating Elements</h1>

<!--@foreach (var forecast in forecasts) {}-->
<!--@if (forecasts == null){}else{}-->

@code {
    // you can create a type
    // populate it and store it in a list 
    // then iterate over the list to populate the data on the front end
 
    // conditional statements can also be used to switch elements within a page
}
